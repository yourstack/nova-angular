# 测试微服务

``` sh
npm i -g parse-dashboard
parse-dashboard --appId dev --masterKey devmk --serverURL http://metapunk.cn:9999/parse --appName NovaDev
The dashboard is now available at http://0.0.0.0:4040/
```