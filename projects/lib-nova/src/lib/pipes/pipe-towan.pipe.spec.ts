import { PipeTowanPipe } from './pipe-towan.pipe';

describe('PipeTowanPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTowanPipe();
    expect(pipe).toBeTruthy();
  });
});
